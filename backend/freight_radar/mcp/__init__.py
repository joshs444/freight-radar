"""The Standpoint Knowledge MCP server — read-only agent access to the store (P1.5).

`tools.py` is the SDK-free tool contract (CI-tested: read-only, provenance-returning).
`server.py` wires it into a FastMCP stdio server (needs the `mcp` extra to run).
"""
