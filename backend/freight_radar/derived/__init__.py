"""The DERIVED tier (P6 capstone) — the reasoning agent's commentary, quarantined.

The honesty firewall extended to AI: nothing in the fact path imports this namespace, and
this namespace imports nothing from the fact path (detect / ingest / wap). The agent can
recombine + order cited facts; it can never own or mutate a number.
"""
