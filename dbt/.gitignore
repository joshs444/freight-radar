# dbt build artifacts — regenerated by every run.
target/
dbt_packages/
logs/
# per-user anonymous telemetry id dbt writes on first run (machine-specific).
.user.yml
# the hermetic CI fixture DB (built from ci/fixtures/*.csv at job time)
freight_radar_ci.duckdb
